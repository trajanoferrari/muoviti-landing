# Copie para .env (nunca commitado) e configure as mesmas
# duas variáveis no Netlify: Site settings > Environment variables.
#
# Onde achar no Supabase: Project Settings > API
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
